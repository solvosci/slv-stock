Adds a *Biological Zone* field to the receiving line, so the operator
can declare which microbiological classification zone a lot was
captured from.

With that declared, each lot's origin resolves to a single sanitary
classification (`A`/`B`/`C`).
