<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_stock_move_operations" model="ir.ui.view">
        <field name="name">stock.move.operations.form (in stock_move_line_unrestricted_location_dest)</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_stock_move_operations"/>
        <field name="arch" type="xml">
            <field name="quantity_done" position="after">
                <field name="warehouse_view_location_dest_id" invisible="1"/>
                <field name="warehouse_view_location_src_id" invisible="1"/>
            </field>
        </field>
    </record>

</odoo>
