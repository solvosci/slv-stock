Preserves Show Operations option in Picking Type, that could be forced to
`true` sometimes across `res.config.settings`.

This is due to standard stock addon behavior when lot, warehouse and location
options are set together.
