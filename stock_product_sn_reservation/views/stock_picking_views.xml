<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_picking_form" model="ir.ui.view">
        <field name="name">stock.picking.form (in stock_product_sn_reservation)</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form" />
        <field name="arch" type="xml">
            <field name="backorder_id" position="after">
                <field name="sn_locked_invisible" invisible="1" />
                <field
                    name="sn_locked"
                    widget="boolean_toggle"
                    attrs="{'invisible': [('sn_locked_invisible', '=', True)]}"
                />
            </field>
        </field>
    </record>

    <record id="view_picking_internal_search" model="ir.ui.view">
        <field name="name">stock.picking.internal.search (in stock_product_sn_reservation)</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_internal_search" />
        <field name="arch" type="xml">
            <filter name="backorder" position="after">
                <separator />
                <filter
                    name="sn_locked"
                    string="S/Ns locked"
                    domain="[('sn_locked', '=', True), ('state', '=', 'assigned')]"
                />
            </filter>
        </field>
    </record>
</odoo>
