<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_move_form" model="ir.ui.view">
        <field name="name">stock.move.form.inherit</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_form"/>
        <field name="groups_id" eval="[(4, ref('logistics_planning_base.group_lp_user'))]" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field
                    name="logistics_schedule_id"
                    readonly="1"
                    attrs="{'invisible': [('logistics_schedule_id', '=', False)]}"
                />
            </xpath>
        </field>
    </record>
    <record id="view_move_form_2" model="ir.ui.view">
        <field name="name">stock.move.form.inherit</field>
        <field name="model">stock.move</field>
        <field name="inherit_id" ref="stock.view_move_form"/>
        <field name="groups_id" eval="[(4, ref('logistics_planning_base.group_lp_manager'))]" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='company_id']" position="after">
                <field
                    name="logistics_schedule_disabled"
                    attrs="{'readonly': ['|', ('state', '!=', 'done'), ('logistics_schedule_id', '!=', False)]}"
                    widget="boolean_toggle"
                />
                <field
                    name="warehouse_partner_id"
                    groups="base.group_no_one"
                />
            </xpath>
        </field>
    </record>

</odoo>
