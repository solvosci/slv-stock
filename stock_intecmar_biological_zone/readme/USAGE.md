1.  Go to *Inventory \> Configuration \> Sanitary Control \> Biological
    Classification* to see the synced classification lines, or open a
    capture zone's own form (*Sanitary Classifications* page) to see
    just the lines that cover it.
2.  A scheduled action runs the sync hourly, together with the
    INTECMAR toxin zone sync.
3.  Go to *Inventory \> Configuration \> Settings \> Biological
    Classification* to change the API endpoint if needed.
