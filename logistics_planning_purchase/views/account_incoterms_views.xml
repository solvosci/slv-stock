<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_incoterms_tree" model="ir.ui.view">
        <field name="name">account.incoterms.tree (in logistics_planning_mgmt_weight)</field>
        <field name="model">account.incoterms</field>
        <field
            name="inherit_id"
            ref="account.view_incoterms_tree"
        />
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="ls_sale_transport_type" />
                <field
                    name="ls_invoice_disabled"
                    widget="boolean_toggle"
                />
            </field>
        </field>
    </record>

    <record id="account_incoterms_form" model="ir.ui.view">
        <field name="name">account.incoterms.form (in logistics_planning_mgmt_weight)</field>
        <field name="model">account.incoterms</field>
        <field
            name="inherit_id"
            ref="account.account_incoterms_form"
        />
        <field name="arch" type="xml">
            <field name="code" position="after">
                <field name="ls_sale_transport_type" />
                <field
                    name="ls_invoice_disabled"
                    widget="boolean_toggle"
                />
            </field>
        </field>
    </record>
</odoo>
