Adds two fields on account.move.line (val_operation, val_operation_origin) to easy visualization
of it's original type operation (incoming, outgoing, in_return, out_return, inv_adjust, scrap,
and price_update) and the name of the operation itself.
